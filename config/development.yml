app:
  name:
  queue:
    render:

server:
  api:
    host:
    port: 3001

  auth:
    steam:
      api_key:
      callback_url: http://localhost:3001/api/auth/callback/steam
    redirect_url: http://localhost:3000

  db:
    host:
    port:
    database:
    user:
    password:

  minio:
    bucket:
    endpoint:
    secure:
    username:
    password:

worker:
  interval:
  concurrent: 2

  db:
    host:
    port:
    database:
    user:
    password:

  minio:
    bucket:
    endpoint:
    secure:
    username:
    password:

  redis:
    url:
