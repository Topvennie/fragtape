services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: fragtape
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - db_data:/var/lib/postgresql/data

  minio:
    image: quay.io/minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: miniominio
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/ready"]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - minio_data:/data

  server:
    image: ghcr.io/topvennie/fragtape/server:latest
    env_file: .env
    ports:
      - 8000:8000
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy

  worker:
    image: ghcr.io/topvennie/fragtape/worker:latest
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy

  recorder:
    image: ghcr.io/topvennie/fragtape/recorder:latest
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy

volumes:
  db_data:
  minio_data:
